API Usage
=============
A short explanation concerning the use of the Sharkbyte Wisp API in Python.

Download the wisp python library
###################################
We will be using my modified version of the wisp.py library, found here: https://pypi.org/project/wisp.py-jimthescientist/1.2.0/

Use the command   ``pip install wispyjim`` to install the package.

Setting up the python file
###########################

Create a file named bot.py, and add the following to the top of the file.

.. literalinclude:: bot.py
  :language: python

Running this file should print the names of all the servers you own.